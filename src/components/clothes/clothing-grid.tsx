// import ClothingItem from "./clothing-item";
// import { getClothingItems } from "./clothing-fetch";

// export default async function ClothingGrid({ gender }: { gender: string }) {
//     const items = await getClothingItems();
//     return (
//         <ul className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-x-2 gap-y-4 max-w-[1400px]">
//             {items && items.length > 0 ? (
//                 <ClothingItem items={items} gender={gender} />
//             ) : (
//                 <p>No items found.</p>
//             )}
//         </ul>
//     )
// }

